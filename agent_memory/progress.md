# Task Progress

## Current Goal

- 目标：为共享 brief 面板 `outputs/storyboard-brief-selector.html` 增加画面尺寸选择。
- 成功标准：新增 `画面尺寸` 单选组，放在选项区最后；默认选择 `9:16 竖屏`；提供 `1:1 方形`、`4:5 竖版信息流`、`16:9 横屏` 和自定义输入；生成 brief 时包含画面尺寸行。
- 范围边界：只修改 brief 面板，不调整 storyboard 生成规则、历史输出目录或其他交付页。
- 停止条件：HTML 内容检查通过，模拟生成文本包含 `画面尺寸: 9:16 竖屏`。
- 验证方式：Node 内容断言、标记顺序检查、默认值生成检查。

## Status

- 当前状态：已完成 brief 面板画面尺寸选择新增与验证。
- 已完成：确认 brief 面板结构；先运行缺失检查并观察到 `missing 画面尺寸 group`；在 `视频时长` 后新增 `画面尺寸` 单选组；验证最后三项为 `平台 / 视频时长 / 画面尺寸`，默认生成行包含 `画面尺寸: 9:16 竖屏`。
- 下一步：等待用户继续使用或提出新的 brief 面板调整。

## Notes

- 用户确认：尺寸选择放在最后。
- 设计选择：把尺寸作为普通 `.group` 追加到最后，复用现有选择按钮与 `updateBrief()` 自动收集逻辑，避免新增脚本分支。
